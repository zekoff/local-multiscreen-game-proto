# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project status

This repository is a bare scaffold: it currently contains only a `README.md` (title only) and an MIT `LICENSE`. There is no source code, build system, package manifest, or test suite yet.

Based on the repo name (`local-multiscreen-game-proto`), the intent is a prototype for a game that spans multiple screens/devices on a local network, but no implementation decisions (language, framework, networking approach) have been made in the repo itself.

When starting implementation work here, confirm with the user what stack and architecture they want before scaffolding — do not assume a framework.
